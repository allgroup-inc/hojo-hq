# glow-ma 現状整理(2026-08-25時点)

作成: ❶システム関係全般セッション。小柳さんとの状況整理のため作成。

## A. 完成・稼働中の機能

| 機能 | 状態 | 補足 |
|---|---|---|
| 企業マスタ(実データ) | ✅ 稼働中 | 約3,210件投入済み |
| スコアリング・ランク付け | ✅ 稼働中 | `recalculateAllScores`で再計算可能 |
| 掘り起こしアラート(即時・日次) | ✅ 稼働中 | Slack通知 |
| レター下書き生成(Claude API) | ✅ 稼働中 | 1社ずつ/ナーチャリング対象一括/**本日追加: 未接触企業の上位N件を一括生成**(まだ未実行) |
| QRコード生成・発送CSV出力 | ✅ 実装済み・**未実施** | 実際の発送はまだ始めていない(小柳さん確認済み) |
| ダッシュボード(ファネル・KPI等) | ✅ 稼働中 | `updateDashboard`で更新 |
| 紹介パートナー管理 | ✅ 実装済み・軽度利用 | 1件登録済み |
| 関係メモ編集(管理画面から) | ✅ 実運用実績あり | 2件 |
| 現場訪問ログ LINE音声記録 | ✅ 稼働中 | 小柳さん・福田さんで実機確認済み |
| BlueBean CTI通話履歴の自動取込 | ✅ 本日デプロイ・動作確認待ち | Slack失敗通知も追加済み |

## B. 本日追加したが、まだ反映・実行していないもの

1. **初回DM一括生成機能**(`generateInitialDraftsForTopRankedCompanies`) — コードはpush済み。**「内容整理してから」という小柳さんのご判断で、まだ実行を保留中**
2. CTI連携のSlack失敗通知・トリガー失敗通知設定 — コードはpush済み、`clasp push`+再デプロイ+トリガー設定がまだ

## C. 既知の不具合(ブロッカー)

**管理画面(admin console)に、小柳さんご自身を含め誰もアクセスできない状態**
- 設定(実行ユーザー・アクセス範囲)を何度も確認したが、正しい設定のまま
- GCPプロジェクトを「標準」に切り替える作業を進めたが、Apps Script側の「プロジェクトを設定」ボタンが反応しない不具合に阻まれ、切り替えは未完了
- 現状、**代わりにスプレッドシートを直接見る運用**で企業一覧・対応履歴は確認できる(見た目が管理画面ほど整理されていないだけで、データ自体は全部見える)
- この不具合の解消は保留中(時間を置いて再挑戦、またはGoogleへの問い合わせが必要)

## D. 未着手・要判断(小柳さんへの確認事項)

1. **CLAUDE.mdへのKGI追記**: 「年間20件以上のM&A成立(2027年8月〜)」を正式に記録してよいか
2. **各チャット(❷❸❹)からの現状報告**: 連携プロンプトをお渡し済み。回答待ち
3. **初回DM生成の対象・件数**: 上記②の回答を踏まえて、誰に・何件手紙を送るか最終決定
4. 個人情報相当データの閲覧範囲拡大(6名程度への拡大時、契約確認が条件。守り部審査記録に記載済み)
5. BlueBean連携アカウントの専用化可否(現状は共用のまま運用でOKとのご回答済み)

## E. 次にやるべき順番(提案)

1. 各チャットからの現状報告(②)を待つ・整理する
2. CLAUDE.mdのKGI追記を確定する
3. 管理画面の不具合(C)は、急ぎでなければ後回しにし、スプレッドシート直接閲覧で当面運用する
4. ②③が固まったら、初回DM生成→内容確認→発送、という実運用フェーズに進む
