# 部品庫 — 作ってきた仕組みを「小分けに取り出す」ためのカタログ

最終更新: 2026-09-07 / 管理: 統括(アカリ) / 見直し期限: 2027-03-07

**目的**: 今まで作った仕組み・考え方・コードを、次の開発や類似システムの
販売(売り切り・手離れ)のときに、探さずすぐ取り出せるようにする。
新しい仕組みが1つ完成したら、この表に1行足す(全体マップと同じ運用)。

**実績**: この方式の第1号は山梨版。沖縄の部品を組み合わせて第1段階を
1チャットで公開まで持っていけた(docs/連携メモ_20260903_山梨版への沖縄資産引き継ぎ.md)。

---

## A. コード部品(そのまま流用できるもの)

| 部品 | 場所 | 何ができる | 取り出すときのコツ |
|---|---|---|---|
| 別地域版ビルダー | `scripts/yamanashi/build_yamanashi.py` | 沖縄資産から別地域版サイトを再現ビルド | 地名置換はすべてassert(検査)つき。新地域はこれを雛形にする |
| データ検証器 | `scripts/validate_fukugiiro.py` + `tests/golden_fukugiiro.json` | 語彙・締切・禁止表現・併給付け漏れを機械検査 | ゴールデンセット(正解サンプル)方式ごと持っていく |
| 併給注意の自動付与 | `scripts/fg_combine.py` | 制度のダブり受給リスクを自動で注記 | キーワード方式で地域非依存 |
| 窓口情報の型 | `scripts/fg_madoguchi.py` | 役場住所・電話・開庁時間+地図ボタン+非断定注意文 | 開庁時間が公式で確認できなければ「要確認」表示 |
| 申請準備シート生成 | `scripts/generate_kit_pages.py` | 電話台本→持ち物→窓口→振込確認の5ステップ印刷物 | 地域依存は窓口情報とヘッダーのみ |
| 計測ラッパー | `site/fukugiiro/assets/fg-analytics.js` | 計測の一元化+自動巡回ボットを計測から除外 | GA4プロパティ切替は設定1行 |
| /go/中間リンク | `scripts/generate_go_pages.py` + skill `go-link-discipline` | LINE誘導の計測つき転送ページ | 転送先を増やすときはイベント名を必ず変える(再発防止メモ) |
| 出荷ゲート | `scripts/shipping_gate.py` | 対外文面の禁止表現・鮮度を機械検査してから出荷 | ホワイトリスト式。新しい出力先は`TARGET_GROUPS`へ登録必須 |
| 置き場所検査 | `scripts/check_repo_scope.py` | 公開リポジトリに置いてはいけない物をCIで検知 | 販売版を作るときの機密混入チェックにも使える |
| 収集パイプライン | `.github/workflows/` + `scripts/` | 国API+自治体サイト巡回→Claude APIで構造化→公開(1日4回) | 対象サイトの規約確認(守り部)が先。robots.txt遵守 |
| 営業管理システム | `glow-ma/`(GAS) | 企業マスタ・スコアリング・アラート・レター下書き・訪問ログLINE音声記録 | **顧客データは絶対に含めない**。コードの型のみ流用。Web Appの実行ユーザー設定に注意(CLAUDE.md GAS規律) |

## B. 運用の型(プロンプト・規程・考え方)

| 部品 | 場所 | 何ができる |
|---|---|---|
| 三名体制(スイシン/ウタガイ/ベッカイ) | `docs/三名体制運営規程.md` + skill `hojo-triangle-review` | 独りよがりを防ぐ議論の型。どの事業にも移植可 |
| 締切3層ルール | CLAUDE.md + skill `hojo-deadline-alert` | SNS=30日以上/LINE=7〜29日/7日未満=次回予告。「約1か月前から」表現 |
| 拡充+鮮度ループ(週2) | Routine(型は山梨連携メモ§2) | 調査エージェント並行→公式照合→非断定JSON→PR→検証待ち更新 |
| おもてなし総点検(月1) | `docs/おもてなし改善ログ.md` | 3ペルソナで実機を歩き「いいや」ポイントを潰す |
| 再発防止メモ方式 | CLAUDE.md末尾 | 同じミスが2度起きたら1行足す。憲法に置くので全セッションに効く |
| 失敗台帳 | `docs/失敗台帳.md` | 失敗の記録と横展開 |
| マルチAI二重照合 | `docs/マルチAI連携セットアップガイド.md` + skill `multi-ai-crosscheck` | Claude+Geminiの独立照合。片方NGなら要確認 |
| LINE運用原稿一式 | `docs/フクギイロ_LINE運用原稿.md` | あいさつ/締切アラート/受け皿応答/診断結果別テンプレ9種 |
| 地雷リスト(横展開時) | 山梨連携メモ§3 | 到達性チェック欠落・年度誤認など、先に踏んだ失敗の回避集 |

## C. スキル(AIへの手順書そのもの)

- 場所: `.claude/skills/`(専用16個)+ `.claude/commands/`(スタイル39個)
- 他リポジトリへの配布: `scripts/update-skills.sh`(コマンドを配るときは同期対象への追加が必要 — 再発防止メモ)
- スキル自体が売り物・納品物になり得る(「AIに引き継げる運用手順書」として)

## D. 類似システムを作って売る(手離れさせる)ときの手順

1. **部品を選ぶ**: この表から必要な行だけ選ぶ(全部渡さない)
2. **機密を抜く**: 顧客データ・APIキー・営業数値・固有名を除去。
   `check_repo_scope.py`の検査を流用して機械チェック
3. **固有名の置換はassert方式**: 山梨版ビルダーと同じく「置換されたことを機械が確認」する形にする(手作業の置換漏れが一番の事故源)
4. **運用の型もセットで渡す**: コードだけ渡すと手離れしない。B(運用の型)とC(スキル)を含めて「AIごと引き継げる」形にすると保守依頼が減る
5. **地雷リストを添付**: 山梨連携メモ§3の形式で「先に踏んだ失敗」を渡す
6. **販売条件・価格は小柳さん決裁**(絶対ルール5)。外部提供前に守り部審査
