# 連携メモ 2026-08-22 ― 保全CRMシステム（❹）→ 軸(kakei-crm)

軸の統一事項（2026-08-22）を受けた棚卸し。**推測で埋めず、不明は「不明」**。PIIの実値は書かない。
※kakei-crm へ書き込み権限が無いため、この内容を軸に置いてもらう（会話に貼付）。控えは hojo-hq/docs/churn/。

## 1. 実体の場所
- リポジトリ：**allgroup-inc/hojo-hq**（★公開）。ブランチ `claude/early-churn-risk-retention-1tu4ej`。
- パス：`scripts/churn/`（コード約45モジュール）／`tests/churn/`（315テスト）／`docs/churn/`（設計・運用）。
- 公開URL：GitHub上のリポジトリのみ（**コードと合成データ・設計のみ**）。**デプロイ先URLは無し**
  （ローカルCLI＋ローカルWebコンソール `127.0.0.1` で動く）。claude.ai プロジェクトとしても進行。

## 2. 稼働状態
- **完成・未投入**。根拠：全機能が合成データで一巡動作（検知→今日の要接触→記録→学習→教育→集約）、
  テスト315件green、独立レビュー通過。**実データは未投入**（統制環境・守り部審査・小柳さん決裁の前）。

## 3. 公開状態
- **公開**。社外の誰でも GitHub 上で**コード・設計ドキュメント・合成データ**を閲覧可能。
- ただし**顧客の実データ・PIIは一切コミットしていない**（コードと合成データのみ・氏名門前禁止をコードで強制）。
- ★**軸の新方針「社内限定・非公開で始める」と不整合**。→ 項目8で軸判断を仰ぐ。

## 4. 顧客の個人情報を持っているか（項目名のみ・実値は書かない）
- **git上には持っていない**（実データ未投入）。設計上、実運用で扱う予定の項目名：
  顧客ID／契約日・受注日／現ステータス／保険料／払込経路／未収履歴／初回引落予定日／年齢／性別／
  市町村／商品／営業担当／**証券番号**／対応記録（**懸念**〔健康状態が混じり得る＝要配慮〕・返し方・結果）。
- **意図的に持たない**：氏名・生年月日・電話番号・住所。

## 5. 独自に持ってしまっている顧客項目
- 氏名・住所などは**持っていない**（データ最小化）。
- 独自の顧客番号は**発番していない**。ただし紐づけキーが**保険管理画面の顧客ID**流用で、
  **KM- 参照はまだ無い**（要是正）。**証券番号**を結合キーとして追加済（読み取りのみ）。

## 6. 他システムとの依存
- 受け取り：保険の**管理画面エクスポート（Excel/CSV）**が主入力。将来 ❸(対面営業)からの申込引き継ぎ、
  ❺(本社ログ)の音声要約から「懸念」を受け取る想定。
- 渡す：現状は**自己完結**（ローカル出力）。将来は 1台帳(KM-)・共通ログイン・❺と同フォーマットの記録に寄せる。

## 7. 止まっていること（何を待っているか）
- **徳元さん(現場)の確認待ち**：架電禁止の欄と種類／証券番号の有無・列名／全社共通番号(KM-)の有無／
  申込日と成立日が別列か（確認メール送付済み想定）。
- **守り部審査＋小柳さん決裁待ち**：フェーズ2（実データを読ませる）＝要配慮個人情報の保管。
- **軸判断待ち**：下記8。
- ※**手動運用は開始可**（徳元さんに手順書を渡すだけ・実データは徳元さんの手元で完結）。

## 8. 軸に決めてほしいこと
1. **★公開リポの扱い**：保全CRMは公開 `hojo-hq` にある。社内限定方針に対し、**非公開リポへ移すか**、
   一度公開したコードの履歴の扱いをどうするか。（PIIは未コミットだが方針上は要判断）
2. **顧客IDの3層対応**：**証券番号（契約単位）／KM-（顧客単位）／保険顧客ID（保険会社単位）**の対応づけを
   v1.0で確定してほしい（保険1顧客が複数契約＝複数証券番号を持つため）。
3. **解約の語彙・目標・起点の更新**：当システムは「早期解約＝契約日から6ヶ月・目標3%未満」で作成。
   軸の新語彙は **超短期解約＝成立から6ヶ月・目標1%以下／短期解約＝成立から12ヶ月・目標3%未満**。
   → **語彙(超短期/短期)・起点(成立日)・目標(6ヶ月=1%以下)に合わせる**でよいか（合わせる前提で改修する）。
4. **要配慮個人情報**：懸念タグに健康状態を記録してよいか（守り部審査と併せて）。

## 共通約束・用語の順守状況
- 【1】生データ持たない＝順守／【2】自己発番しない＝順守（KM-参照は未・要是正）／
  【3】自前ログイン作らない＝順守（共通ログインに乗る）／【4】用語言い換えない＝順守（超短期/短期解約に合わせる）。
