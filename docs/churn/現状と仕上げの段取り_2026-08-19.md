# 早期解約 保全システム ― 現状と仕上げの段取り（2026-08-19）

散らかった論点を1枚に。**結論：機能はほぼ完成。残るのは"実データで動かす人手ゲート"と"1アプリ合流"だけ。**

## いまの立ち位置（3行）
- 検知→優先→行動→記録→学習→教育→集約まで**合成データで一巡動作**（テスト全通過・独立レビュー通過）。
- **止まっているのはコードではなく、実データを扱う段取り**（統制環境・人手）と、**1アプリ合流のID合わせ1点**。
- 現場の手動運用は**今日から本物データで始められる**（徳元さんの抽出リストに"拾う順"を足すだけ）。

## できていること（機能マップ・全部ある）
| 段階 | 機能 | 状態 |
|---|---|---|
| 検知 | 現ステータス分類・未収(Ⅳ)・リスク採点・トリガー(未払消滅目前/不着/遅延/未収2連続/再発/口座確認/初動) | ✅ |
| 優先 | 今日の要接触（優先度順×守れる金額） | ✅ |
| 行動 | 架電／訪問エスカレーション＋今日の一手 | ✅ |
| 記録 | 保全コンソール（記録一体・ローカル）＋対応記録の追記 | ✅ |
| 学習 | 段階導入で因果（imminent-uplift/relapse-uplift）・遷移(救えた/失った)・ハザード第2の山 | ✅ |
| 教育 | 懸念×返し方の教育カード（talkscript） | ✅ |
| 集約 | 保全ダッシュボード（3%スコアボード・今日・山・再発・LTV補助） | ✅ |
| 保管 | 月次スナップショット＋保持6年＋削除監査 | ✅ |

## 仕上げに残っていること（3本＋後回し）

### A. 現場の手動運用を始める（今週・実データ・ソフト不要）
- 徳元さんに `徳元さんへ_保全のはじめかた.md` ＋ 見本CSV を渡す → **来月10日以降の架電から"拾う順"で回す**。
- **オーナー：小柳さん（渡すだけ）**。審査待ち・ソフト不要（既存業務の延長）。

### B. システムの本領を出す＝実データを統制PCで読ませる（フェーズ2）
- これで「新規の即時発見(リスク採点)・当たり具合(AUC)・解約の集中時期・ダッシュボード」が**本物の数字**で動く。
- 手順：統制されたPC（徳元さんのPC等）にコードを置く → 実CSVを column_map で読む → 実行。**エンジニア不在の分は
  Claudeが導入から伴走**（Python導入→コマンド1行）。データはそのPCから出さない・出力は private。
- ここで **B群業務定数（AUC_MIN・帯・金額区分）を実データで確定**（小柳さん決裁）。
- **オーナー：小柳さん＋Claude（伴走）／作業者＝徳元さん（統制PC）**。

### C. 1アプリ合流の準備（IDと言葉を合わせる）
- 「1つのアプリ・1つの顧客台帳(kakei-crm)」へ合流するための**唯一の是正点＝紐づけIDをKM-に合わせる**。
- 必要な確認（1つ）：**保険CSVにKM-番号が入っているか／別に対応表があるか**。
- 生データ(氏名・電話・住所・生年月日)は持たない／独自ログインは作らない＝**すでに順守**。
- **オーナー：軸(総合管理)への確認 → Claudeが取込に反映**。

### 後回しでよいもの
- #142の**外部専門家レビュー**（利用目的・保持6年）。小柳さん判断で先行済み・後日推奨。
- **月次自動化**（10日以降＋新規随時）。手動が回ってから。
- **訪問・減額の運用細部**（守り部は回答済＝解禁。しきい値・一手は実データの手応え後）。
- **A5 安心コール**（"あれば良い"施策）。

## 段取り（時系列・誰が）
```
今週      A 徳元さんへ手順書 → 手動架電スタート（本物データ・小柳さん）
 ├並行   C KM-の有無を軸に確認（小柳さん→軸→Claude反映）
 └並行   #142 外部専門家レビュー依頼（任意・小柳さん）
次       B 統制PCで実データ実行（徳元さんPC＋Claude伴走）→ AUC等を見る
         → B群定数を確定（小柳さん決裁）→ コンソールを現場の入口に
その後    月次自動化 / 訪問・減額の運用 / 1アプリ合流（共通ログイン・KM-台帳）
```

## 「完成」の定義（ここに来たら仕上がり）
1. 現場が**保全コンソールを毎日開いて**、今日の要接触を上から回している。
2. 対応記録が貯まり、**教育カード・効果測定（段階導入）が回っている**。
3. **早期解約率が3%未満**で推移していることを、コホートで測れている。
4. 顧客は**KM-で1台帳**に紐づき、1アプリの1画面として動いている。

## 次の一手（推奨）
**Aを今週開始**しつつ、**Bのための"超やさしいセットアップ手順"をClaudeが用意**（フェーズ2の入口）。
Bが動くと"新規の即時発見"が本物で動く＝このシステムの本領。Cは軸への1問で解ける。
