# 保全CRM(❹) → 軸への報告：5入口・確定版を受けて（2026-08-19）

【⭐️共通認識・確定版「1つのアプリを5つの入口で」】を受けた、❹保全CRMの回答（4問）と、
確定版で見えた**言葉・データの合わせどころ**。判断基準＝「見込みが埋もれるのを防ぐ／効率を上げる」。

## 4つの質問への回答

### 1. 使う人／1日に何回開くか
- **保全部：徳元さん＋部下3名（増員予定・将来は対面保全も）**。
- **毎営業日・常時**（朝いちに「今日の要接触」を見て、架電しながら記録）。実質"開きっぱなし"で1日通して。

### 2. 手入力させていること／自動化できるもの
- **いま手入力**：架電後の対応記録。ただし**接触日以外は選択式（押すだけ）**：手段・懸念・返し方・結果・次アクション。
- **自動化済/自動化する**：
  - **誰に架電すべきか＝完全自動**（トリガーで today リストが出る＝入力不要）。
  - 接触日＝今日で自動。現ステータス・未収＝管理画面/台帳から取込（手入力しない）。
  - **将来❺の音声要約から「懸念・話した内容」を自動反映**すれば、選択入力すら減らせる（＝入力を仕事の副産物に）。
- **二重入力の回避**：営業(❸)が聞いた懸念・家族構成を、**保全(❹)は再入力しない**＝台帳/❺から引き継ぐ前提。

### 3. 開いて最初に出るべきもの（今日やること）
- **今日フォローすべき契約が最初から出る**（探させない・検索させない）：
  - 未払消滅目前＞不着＞遅延＞未収2連続＞再発 ＞（新規の初動・口座確認）／各帯は守れる金額順。
  - ＋**放置が自動浮上**（＝核心）：14日さわっていない・**後確認が未済**・解約リスク高。
- **架電禁止・対象外は最初から出さない**。

### 4. 現場の手間が増える箇所（正直に）
- **懸念**：架電54件×5項目選択は、忙しいと"入力のための手間"になり得る。
  → **対策：必須は「結果区分」1つだけ**、懸念・返し方は任意（分かるときだけ）。将来は❺音声要約で自動化。
- **放置浮上が今は集約ビュー側**にある。→ **"今日やること"に放置浮上を最初から混ぜる**（改善予定）。

## 確定版で見えた「合わせどころ」（要是正・要確認）
| 項目 | いまの状態 | 合わせる方向 |
|---|---|---|
| **ステータス語彙** | うちは**保険会社の契約ステータス**（成立済／引受OK／未払消滅…）で管理 | 共通の**流れステータス**（申込/成立/保全中/CRM対象）とは**別の軸**。うちの契約は全部「CRM対象／保全中／成立」の中の詳細。**共通語彙は言い換えず**、保険ステータスは"保全の内部詳細"として保持（対応を軸と確認）。 |
| **申込≠成立** | 現ステータスで「成立済」等を判定済 | 共通定義（申込=書類提出／成立=引受完了）に合わせて用語整理。 |
| **架電禁止** | **未対応**（対象外＝死亡/解約系のみ除外） | **架電禁止フラグを台帳から読み、today リストから必ず除外**（持ち場無関係・最優先）。→ 実装すべき（台帳連携C）。 |
| **送信型後確認(SMS)** | **未実装** | ❹の機能。「成立後、後確認が未済の契約」を**自動浮上**させる（principle4）。→ 足す候補。 |
| **顧客ID＝KM-** | 保険管理画面のIDを流用 | **KM-参照 or 対応表**（軸に要確認：保険CSVにKM-が入るか）。 |

## 4つの共通約束の順守（再掲）
- 【1】生データ（氏名・電話・住所・生年月日）は**持たない**＝順守。
- 【2】自己発番**しない**（保険IDを流用）＝KM-参照に是正予定。
- 【3】自前ログイン・権限を**作らない**＝順守（共通ログインに乗る）。
- 【4】ステータス語彙を**言い換えない**＝共通語彙(申込/成立/保全中/CRM対象)はそのまま使う。

## 是正の優先順（❹として）
1. **架電禁止フラグの除外**（安全・信頼に直結）＝台帳の該当項目が分かり次第すぐ。
2. **KM- 参照**（合流の前提）＝軸に「保険CSVにKM-が入るか」を確認。
3. **記録の必須を結果1つに／放置浮上をtodayに統合**（現場の手間を増やさない）。
4. **送信型後確認（後確認未済の自動浮上）**＝機能候補（見込みが埋もれるのを防ぐに合致）。

※実データ処理は統制環境・#142審査後。合成データでの検証・設計は先行可。迷う点は軸へ確認。
