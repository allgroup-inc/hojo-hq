# 徳元さん回答の反映（2026-08-22）― 架電禁止・証券番号・KM-・申込/成立日

徳元さんの4回答と、システム反映・軸への申し送り。

## 4回答と反映
| # | 質問 | 徳元さん回答 | 反映 |
|---|---|---|---|
| 1 | 架電禁止の欄 | **分かる欄はない** | ★**データに架電禁止フラグが無い＝システムでは自動除外できない**。→ 軸へ申し送り（下記）。当面は現場の判断・募集人の架電システム側に依存。 |
| 2 | 証券番号の列 | **今のExcelには無いが抽出は可能** | 結合キーとして`policy_number`は追加済（読み取り）。**次回抽出から証券番号列を含めてもらう**（列名は抽出時に確定）。申込時の控えも今日から。 |
| 3 | 全社共通番号(KM-) | **無い**。顧客IDは募集人の架電システムで紐づけているID | KM-はこのデータに無い。**KM-対応は台帳(kakei-crm)側の突き合わせが必要**（証券番号 or 保険顧客ID ↔ KM-）。→ 軸へ。 |
| 4 | 申込日/成立日 | **別々に抽出可能** | **`settle_date`（成立日）をschemaに追加**（実装済・test通過）。軸定義の解約起点＝成立日に移行できる状態に。当面の判定基準は既存のまま、移行は軸のA-2確定後。 |

## 軸への申し送り（この回答で確定した論点）
1. **★架電禁止の運用（安全）**：**現データに架電禁止フラグが無い**。軸判断2の「区別つくまで全件除外（安全側）」は
   除外の元になるフラグ自体が無いため実行できない。→ **架電禁止は台帳(kakei-crm)のどこで持つか／
   募集人の架電システムから連携できるか**を軸に確認したい。それまで**自動生成リストは架電禁止を保証できない**
   （現場の人手判断が前提）。
2. **KM-対応**：このデータに全社共通番号は無い。**証券番号（契約単位）・保険顧客ID（募集人システムのID）↔ KM-**の
   突き合わせ方を台帳側で確定してほしい（軸 A-3）。
3. **証券番号・成立日は抽出可能**＝結合キーと解約起点はデータで用意できる。前進。

## いま前に進められること
- 証券番号・成立日を含めた抽出フォーマットが来たら、`column_map` に `policy_number`＝証券番号列、
  `settle_date`＝成立日列 を足すだけで取り込める（コードは対応済み）。
- 架電禁止は軸の回答（台帳での持ち方）が来るまで**自動除外は保留**、現場判断で運用。
