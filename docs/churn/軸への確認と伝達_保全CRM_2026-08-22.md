# 軸（家計の見直しやさん総合システム／kakei-crm）への確認・伝達 ― 保全CRM(❹) 2026-08-22

このチャット（❹保全CRM）から軸に決めてほしいこと・教えてほしいこと・報告を1枚に。
詳細な棚卸しは `連携メモ_20260822_保全CRM.md`。ここは連絡の要点だけ。

## A. 軸に決めてほしい（方針・決裁）
1. **★最優先：公開リポの扱い**
   保全CRMは公開 `allgroup-inc/hojo-hq` にある。社内限定方針に対し、**非公開リポへ移すか／一度公開した
   コード履歴をどうするか**。※PIIは一切コミットしていない（コード・設計・合成データのみ）。判断を仰ぐ。
2. **解約の語彙・目標・起点を軸定義に合わせてよいか**
   当システムは「早期解約＝契約日から6ヶ月・目標3%未満」で作成。軸の新定義＝
   **超短期解約（成立から6ヶ月・目標1%以下）／短期解約（成立から12ヶ月・目標3%未満）**。
   → **用語(超短期/短期)・起点(成立日)・目標(6ヶ月=1%以下)に合わせて改修**でよいか。
3. **顧客IDの3層の対応づけ（v1.0で確定してほしい）**
   **証券番号（契約単位）／KM-（顧客単位）／保険顧客ID（保険会社単位）**。
   保険1顧客が複数契約（複数証券番号）を持つため、3層をどう繋ぐか。
4. **要配慮個人情報（健康）の記録可否**
   保全の「懸念」タグに健康状態が混じり得る。記録してよいか（守り部審査と併せて）。

## B. 軸に教えてほしい（仕様）
5. **共通ログイン・権限モデルの仕様**（閲覧=従業員／変更=数名／システム=小柳さん）。
   保全コンソールを**自前ログインを作らず**この共通認証に乗せたい。連携方法を教えてほしい。
6. **❺（本社ログ）と同じ記録フォーマットの仕様**（項目名・保管様式）。保全の対応記録をこれに合わせる。
7. **保険顧客ID ↔ KM- の対応表の在り処**（KM-が保険CSVに入らない場合、どこで突き合わせるか）。
8. **台帳での「架電禁止」の表現**（項目・値・種類）。「勧誘お断り／一切連絡不可」を区別できるか。
   ※現データ側は徳元さんに確認中。台帳(kakei-crm)側の正の定義を軸に確認したい。

## C. 軸へ報告（FYI・対応済／現状）
- **共通プロンプト4質問の回答＝提出済**（使う人／手入力と自動化／最初に出すもの／手間が増える箇所）。
- **8項目棚卸し＝提出済**（`連携メモ_20260822_保全CRM.md`）。
- **現状：完成・未投入**（合成データで一巡・テスト315green・独立レビュー通過）。**手動運用は開始可**。
- **軸の判断を反映済**：記録は結果1つで1タップ／要フォローは"日数"でなく"やることが済んでいない"で浮上／
  AUC合格ライン0.65を実データ前に固定／証券番号を結合キーに追加／申込のとき証券番号を控える依頼。
- **共通約束の順守**：生データ持たない／自己発番しない（KM-参照は未・是正予定）／自前ログイン作らない／
  用語言い換えない（超短期/短期解約に合わせる）。
- **PR #7（hojo-hq）CI green**。※公開リポ上＝A-1に該当。マージ・可視性変更は軸判断待ち。

## 独自に決めないこと（軸へ上げる／このチャットでは動かさない）
- 顧客ID体系・項目名の追加変更・権限モデル・共通用語・外部連携(CTI/SMS/メール/AI API)・保存場所/エクスポート。
  → いずれも上のA/Bで軸に確認してから。合成データでの設計・検証のみ先行。
