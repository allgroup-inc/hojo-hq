# 小柳さん 今日やること（早期解約 保全システムを前に進める4送信）

コード・手順・依頼書は全部そろっている。あとは**この4つを送るだけ**。
最優先は「1」。他は全部この上に乗るので、1が動かないと先に進まない。3・4は1と同時に出してよい。

---

## 1（最優先）｜技術担当に「環境構築＋初回実行」を依頼
- **誰に**: 技術担当（情シス or エンジニア）
- **渡す**: `docs/churn/初回実行_依頼書.md`（＋参考 `hojo-churn_セットアップ手順.md`）。ブランチ `claude/early-churn-risk-retention-1tu4ej`。
- **そのまま送る文面**:
  > 早期解約の保全システム、実データで1回だけ動かして設定を確定したいです。手順は添付の依頼書のとおり（半日想定）。**統制された社内マシンで**実行し、実データは公開リポ・個人PCで扱わないでください。preflightが安全に止まるので、列名を合わせて再実行すればOKです。**返してほしいのは AUC / ECE / precision@30 の数字と、保険料のざっくりした件数分布だけ**（顧客情報は共有不要）。
- **完了の合図**: 上記の数字が返ってくる → これで残りの設定（B群定数）を確定できる。

## 2｜データの置き場所を決めて徳元さんに案内
- **まず情シス・守り部に**:
  > 氏名を抜いた顧客Excelを1つ置く、社内の管理された共有先（アクセス制御・ネット非露出）を用意してください。外部サービス・メール添付は不可。
- **場所が決まったら徳元さんに**:
  > 先日の氏名なしExcelを、◯◯（決めた場所）に置いてください。特別な加工は不要です（現ステータス・Ⅳ・初回引落予定日はそのままで大丈夫）。
- **完了の合図**: 実データが統制環境に置かれる → 手順1の技術担当が実行できる。

## 3｜外部専門家レビュー（#142・1と並行で今すぐ）
- **誰に**: 顧問弁護士 or 個人情報保護士
- **渡す**: `docs/churn/守り部審査_回答パッケージ.md`
- **そのまま送る文面**:
  > 保険契約の解約予兆データを継続的に蓄積・分析する運用について、2点だけ見てください。①いまの取得目的の範囲で「継続的な分析・保全のための蓄積」が説明できるか（追加公表・同意の要否）。②予兆データの保持年数6年は妥当か。
- **完了の合図**: 指摘があれば利用目的の文言・保持年数を直す（コード側は即対応可）。

## 4｜守り部確認（訪問保全・募集規制・1と並行で今すぐ）
- **誰に**: 守り部
- **渡す**: `docs/churn/守り部確認依頼_訪問保全と募集規制.md`
- **そのまま送る文面**:
  > 保全に「訪問」と「対話ログ」を足しました。募集規制まわりで5点だけ判定してください。①減額提案は"相談のきっかけ"提示に留め提案・成立は有資格者、で可か ②訪問・保全対応は有資格者に限定するか ③引き止め・威圧の禁止表現の監修 ④保全訪問で乗換勧誘しない明文化 ⑤収集する懸念タグの範囲。
- **完了の合図**: 5点の可否＋禁止表現の監修 → 訪問保全・減額提案(B1)を実運用に出せる。
  ※**A1〜A3の架電・手続き支援は #142 の枠内で先行可**。守り部確認を待つのは「訪問」と「減額提案」だけ。

---

## 進み方（全体像）
```
1 環境構築 ─┬─→ 2 データ受領 ─→ 初回実行 ─→ AUC等の数字 ─→ B群定数を確定(小柳) ─→ 段階導入で運用開始
            │
(並行) 3 専門家レビュー / 4 守り部確認 … 済み次第、保持年数の是正・訪問/減額の解禁
```
- **ボールはいま「小柳さん → 技術担当」**。まず1を送るのが最短。
- 環境ができて数字が返ってきたら、私（Claude）が B群定数の確定と段階導入の設計を即対応します。
