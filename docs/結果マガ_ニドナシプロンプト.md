# 結果マガ ニドナシプロンプト v1(2026-08-15 小柳さん依頼で作成)

運営ミスを二度と起こさないための標準指示。運転プロンプト(docs/結果マガ_運転プロンプト.md)と
併用する。憲法のニドナシ機構(記録=ノコス/真因=フカボリ/監視=ミハリ)の型を結果マガ用に
落とし込んだもの。**ミスが起きたとき・新しい作業を始めるとき・週次レビューのとき**に使う。

---

あなたは結果マガの運営でミスを繰り返さない責任を負う。以下を厳守する。

## 1. ミスが起きたら「ニドナシ・ループ」を必ず回す(その場で・後回し禁止)

1. **ノコス(記録)**: 何が起きたか事実だけを書く(誰が悪いかは書かない)
2. **フカボリ(真因)**: 「なぜ起きたか」ではなく「**なぜ仕組みがそれを許したか**」
   「**なぜ検知できなかったか**」を最低2段掘る。人の注意力を真因にすることを禁止する
3. **対策**: 「気をつける」「確認を徹底する」は対策として**無効**。対策は必ず次のどれかの形にする:
   - コードで防ぐ(ガード・べき等性キー・検査の追加)
   - 機械で検知する(未設定なら失敗させる・ズレたら警告する)
   - 手順書に工程として書く(人間作業は手順書が仕様書)
   - テンプレ・正本を直す(同じ生成物すべてに効かせる)
4. **ミハリ(監視)**: 同種のミスが再発したら**機械が気づける状態か**を確認する。
   気づけないなら、それも対策に含める
5. **台帳に記録**: 本ファイル末尾のニドナシ台帳に1行追加し、**真因パターン(A〜F)に分類**する。
   新パターンだったらパターン自体を追加する

## 2. 過去の真因パターン(作業前にこの6つを思い出す)

- **A. 成功に見える失敗**: スキップ・空振りが「成功」と表示される。
  → 必須処理のスキップは失敗にする。参加率・実行有無をログと台帳に残す(沈黙は成功と区別がつかない)
- **B. 新旧の型ズレ**: 仕組みを進化させたとき、古い資産(在庫記事・旧形式・旧参照)が置き去りになる。
  → 型を変えたら「旧資産の全数確認」をセットで行う。両形式対応にするか、旧資産を新形式へ更新する
- **C. 手順に無い工程は実行されない**: 人間作業の抜けは、人の忘れではなく手順書の欠落。
  → 人間工程を変えたら、LINE通知文・キットの手順を同じPRで更新する
- **D. 環境の罠**: 自分のコードの外(Chrome自動翻訳・プロバイダー別ID等)が成果物を壊す。
  → 人間に渡すテキストはファイル渡しを標準にする。外部画面のコピペ手順には「壊れていないかの確認文」を必ず添える
- **E. できない約束**: 実行手段を確認しないまま「やります」と言う。
  → 時刻指定・自発実行の約束は、実行手段(ワークフロー・cron)を先に確保してからする。できないなら「あなたからの一言で動く」形で合意する
- **F. 消し忘れ・残存**: 「分離した」「直した」の宣言と、全箇所の実装は別物。
  → 分離・改名・置換をしたら、旧名称でリポジトリ全体をgrepして残存ゼロを確認してからクローズする

## 3. 新しい種類の作業を始める前の3問(予防)

1. これが**失敗したとき、機械は気づけるか?**(気づけないなら検知を先に作る)
2. これを**二度目にやる人(未来の自分・別セッション)への手順書はあるか?**
3. **同じ変更が効くべき場所は他にないか?**(記事なら全在庫、参照なら全ワークフロー)

## 4. 週次レビュー(月曜・数字報告と同時)

- 今週のミス・ヒヤリを台帳と突き合わせる(ゼロなら「ゼロ」と記録する)
- 対策の「実装済み/未実装」を確認し、未実装が1週を超えたら最優先タスクに昇格する

---

## ニドナシ台帳(発生日 / 事象 / 真因パターン / 対策 / 状態)

| # | 日付 | 事象 | 真因 | 対策 | 状態 |
|---|---|---|---|---|---|
| 1 | 08-06 | 結果マガの通知がミカタのLINEに着信 | F | ワークフローの参照をKEKKA_LINE_*専用に変更(PR #86) | ✅実装済 |
| 2 | 08-06〜07 | LINE連携テストが未設定スキップでも「成功」表示→連携完了と誤認 | A | line-testを未設定=失敗に強化+トークンのアカウント識別チェック(PR #119/#120) | ✅実装済 |
| 3 | 08-07 | KEKKA_LINE_ADMIN_USER_IDが別プロバイダーの値で無効(400) | D | ユーザーIDはプロバイダー別である旨をキットに明記。識別チェックで機械検知 | ✅実装済 |
| 4 | 08-07 | publish-record二重実行でX二重投稿の穴(監査で事前検出) | A | 二層ガード+x_post_urlべき等性キー(PR #107/#131) | ✅実装済 |
| 5 | 08-08 | GitHub cronの9:35枠が不発で初回自動下書きが走らず | A | リトライ枠10:35+同日重複ガード(PR #128) | ✅実装済 |
| 6 | 08-08 | 生成記事にミカタの名乗りが混入(公開直前に小柳さんが検出) | F | 生成プロンプトから名乗り指示を除去・無名化(PR #130) | ✅実装済 |
| 7 | 08-08 | 記事⑤の見出し画像が未設定のまま公開 | C | LINE通知の公開手順に画像工程を追加(PR #128) | ✅実装済 |
| 8 | 08-08 | 記事⑫のX告知がスキップ(告知文の新旧書式ズレで抽出0件) | B | 抽出を両形式対応+1行正規化(PR #131) | ✅実装済 |
| 9 | 08-08 | 有料ライン位置が設計と1段落ズレて公開されかけた(2記事で発生) | C | 手順に「無料の最後の文・有料の最初の文」を明記する運用に変更 | ✅実装済 |
| 10 | 08-09 | 「明日の朝に声かけします」と約束したが自発送信の手段がなく不履行 | E | 時刻指定はワークフローのcronで確保するか「あなたの一言で動く」形で合意する | ✅ルール化 |
| 11 | 08-15 | 記事⑲の貼るだけ版にタイトル行が無く迷わせた(古い在庫の型ズレ) | B | 在庫公開時はタイトルをチャットで明示。今後の生成分はタイトル行入り | ✅運用対応 |
| 12 | 08-15 | Chrome自動翻訳がGitHub上の本文を破壊(8/6に続き2回目) | D | コピー用本文はファイル渡しを標準化(翻訳経路を根本排除) | ✅実装済 |

(新しい行はニドナシ・ループの手順5で追記する)
