# 受給報告フロー 運用手順(2026-08-10 稼働開始)

北極星❷「受給できた報告1,000件」の受け口。利用者が https://allgroup-inc.github.io/hojo-hq/fukugiiro/houkoku/ から
「もらえた報告」をLINEに送り、御礼を返し、台帳に貯める。

## 流れ(全体図)
1. 導線: 診断結果 / 申請準備シート(252ページ) / トップ / 市町村別まとめ(41) / ライフイベント別(9) から報告ページへ
2. 報告ページで 制度名・市町村(任意)・ひとこと(任意) を入力 → 「LINEで報告を送る」
   - LINEベーシックID設定後: LINEが開いて本文が自動セット(ワンタップ)
   - 未設定の間: 本文が自動コピーされ「LINEを開いて貼り付けて送る」案内(現在はこちら)
3. 御礼を返す(下の文面をコピペ・当面は手動)
4. 台帳に転記: `python scripts/add_jukyu_report.py --seido-id ... --status 計上` (検証部が公式制度と突合できたら --verified)
5. 検証済み件数はサイトの実績カードに自動表示(0件の間は非表示)・週次レポに自動集計

## 御礼メッセージ(**A案で決定** 2026-08-10 小柳さん決裁・報告が届いたらこの文面を返信)
**A案(あたたかい沖縄トーン)= 採用**
> ご報告、にふぇーでーびる(ありがとうございます)🌈
> 「もらえた」の一言が、次の誰かの「調べてみよう」につながります。
> これからも締切や新しい制度を、そっとお知らせしますね。
> あなたが受け取るまで、いっしょに。— もらいわすれ堂

**B案(簡潔・丁寧)**
> ご報告ありがとうございます。無事に受け取れて何よりです。
> いただいた内容は、個人がわからない形で「次の誰かのもらい忘れ防止」に役立てます。
> 引き続き、締切や新しい制度をLINEでお知らせします。— もらいわすれ堂

## 小柳さんにお願いする設定 → **すべて完了(2026-08-10)**
1. ~~LINEベーシックIDの共有~~ → **完了**: FG_LINE_OA_ID設定済み・ワンタップ送信 稼働(PR#163)
2. ~~御礼文面 A/B の選定~~ → **A案で決定済み**(報告が届いたら受け皿応答の自動返信に続けてA案を手動返信。将来Messaging API連携で自動化予定)
3. **受け皿応答も稼働**(2026-08-10): 一律応答で診断結果・もらえた報告の貼り付けに即時返信、チャット併用オン(docs/フクギイロ_LINE運用原稿.md §E)。実機トークで表示崩れなし確認済み

## 実績カードの公開タイミング(2026-08-10 小柳さん決裁)
検証済み報告が **10件たまってから** トップに表示する(1件目から出すと寂しく見えるため)。
実装: LP側で `summary.household_count >= 10` のときのみ実績カードを表示。それまでは非表示のまま台帳に蓄積。

## PII・正確性ルール(守り部)
- 報告ページは「名前・住所・金額を書かない」を明記。台帳にも個人特定情報は入れない(household_keyは匿名キーのみ)
- サイト計上は **検証部が公式制度と突合できた件数のみ**(自己申告のままの数字は公開しない)
- 台帳: data/fukugiiro/jukyu_reports.json(summaryのdisclaimer参照)

## 計測イベント
- jukyu_report_link_top / _shindan / _kit / _area / _life: 各導線のクリック(=報告ページへの到達前クリック)
  - 2026-09-19 追加: 市町村別まとめ41ページ・ライフイベント別9ページに導線が無く、報告ページへ辿り着けなかった。
    単一CV(LINE登録)と競合させないため、ボタンではなく末尾の控えめな一文にしている(絶対ルール4)。
  - 申請準備シート「一覧」ページにも導線が無かったため追加(`jukyu_report_link_kit` に `{pos:"index"}` を付け、
    252枚の個別シートと区別できるようにした)。
- jukyu_report_click: 報告ページで「LINEで報告を送る」を押した数(=送信意思。週次レポ集計対象)

**2026-09-19 修正**: 診断ページの導線が `jukyu_report_click` を発火しており、遷移クリックが
送信意思として数えられ、さらに報告ページで送信すると二重計上されていた(週次レポの「受給報告」欄)。
仕様どおり `jukyu_report_link_shindan` に修正。週次レポも「報告ページへ(内訳付き)」と
「報告の送信」を分けて表示するようにした。**導線クリックと完了を同じイベント名で数えない**。

**2026-09-19 GA4移行**: Plausible契約終了(8/24)以降 `data/fukugiiro/funnel.json` は
2026-08-20 で欠測していた。`fetch_plausible_funnel.py` をGA4優先に変更(Secrets:
GA4_SA_JSON / GA4_PROPERTY_ID)。**Secretsが登録されるまで数字は復旧しない**(決裁キュー U3)。
