# 失敗台帳 — ニドナシ機構管理(削除禁止)

運用: ノコスさん(記録)→フカボリさん(真因・対策)→ミハリさん(実装検証・再発監視・クローズ判定)
原則: 人を責めない・隠さない・忘れない。自己申告は功績。「気をつける」は対策ではない。
クローズ条件: 仕組みで担保+再現テスト合格+6ヶ月無再発(機構3名の三名体制議論で判定)

## ステータスサマリー
| 記録 | 分析中 | 監視中 | クローズ | 再発(目標:常にゼロ) |
|---|---|---|---|---|
| 5 | 1 | 4 | 0 | 0 |

## 台帳

| ID | 発生日 | 分類 | 事実経過 | 影響 | 発見経路 | 真因 | 対策(形態) | 横展開 | ステータス | 見直し/クローズ予定 |
|---|---|---|---|---|---|---|---|---|---|---|
| FK-004 | 2026-08-11 | 事故+プロセス | うごくAIレシピの自動生成が8/11に2回連続失敗(生成レシピのprompt_text内に禁止語「必ず」→バリデーションが正しく却下・持ち越し。ガードレール自体は設計どおり機能)。失敗が3日間誰にも見えなかった(専用LINE未設定のため失敗通知の送り先が無く、resilient-agent-designが警告する「静かな脱落」が現実化)。2026-08-17の点検(新スキル・コマンド導入後の総点検)で発見 | 火曜枠の記事が3日間欠落(8/14に自然回復・実害は限定的) | 自己申告(2026-08-17点検で発見) | ①禁止語検査がAIへの指示文(prompt_text)まで検査対象にしていた設計の穴 ②失敗時の通知先(専用LINE)が未設定のまま運用開始していた | ルール化+実装: 禁止語検査を読者向けの文に限定(prompt_text・実行ログ原文引用は対象外)。不合格時は理由フィードバック付き再生成を1回まで許可(上限つきリトライ)。専用LINE未設定時はGitHub Issueを自動起票(沈黙禁止・`permissions: issues:write`追加)。議事: `docs/議事_airecipe監査_2026-08-17.md`(三名体制・ウタガイ反対理由記録済み) | 他の自動生成系(generate_sns.py等)にも「失敗時の通知先未設定=サイレント」パターンがないか要確認 | 監視中 | 2027-02-11 |
| FK-003 | 2026-07-26 | 事故+プロセス | Lighthouse CIがperformanceスコア基準(0.95)未達で連続失敗し、GitHub Issue #10が21日間(〜2026-08-17)開いたまま自動botコメントが積まれ続けたが、対応着手されなかった。CLAUDE.md記載によれば2026-08-05・08-12にレイアウト系CSS(`word-break:auto-phrase`等)を見出し以外の繰り返し要素にも適用する変更が入り退行を悪化させていた(0.88〜0.91)。2026-08-17にPR#205で`h1`〜`h3`限定へ修正し**ローカル実測では**0.98〜0.99へ回復したと報告されたが、修正マージ直後(同日)に本番URLへ`lighthouse.yml`を再実行(workflow_dispatch)したところ**依然0.86でfail**(Issue #10継続)。ローカル実測と本番CI実測の乖離が残っており、真の解決は未確認。※07-26時点の最初の退行要因そのものは本項執筆時点で未特定 | 21日以上、パフォーマンス基準未達のまま本番公開が継続(検索流入・SEOへの実影響は未計測) | 監査(Lighthouse自動CIのIssue起票。対応着手は2026-08-17の点検時) | word-break:auto-phrase等レイアウト系CSSの適用範囲を「効果測定なしに広げる」変更が2度(08-05・08-12)まで防止されなかった。自動Issueへの応答フローが存在せず21日間放置された。加えて、ローカル実測(開発者PC)と本番CI実測(GitHub Actions共有ランナー)のスコアが一致しない構造的問題が今回新たに判明(要因未特定: ランナーのCPUスロットリング/ネットワーク遅延等の推測はあるが未検証) | 部分実装+要継続対応: 適用範囲をh1〜h3見出し限定に修正済みだが本番スコアは未回復。CLAUDE.mdに「レイアウト系CSSは見出し限定。適用範囲を広げる変更はLighthouseローカル実測(hojo-lighthouse-triageスキル)とセットで行う」を明記(2度目の再発のため)。議事: `docs/議事_20260817_verify照合判定基準.md`(三名体制・ウタガイ反対理由記録済み、隣接論点として同日対応) | 同型のCSS変更(text-wrap:balance等)を横断的に見出し限定ルールへ統一。自動起票Issueが一定期間(例: 72時間)応答なしの場合に本台帳へ自動アラートする仕組みを検討。**ローカル実測と本番CI実測が乖離する場合の切り分け手順をhojo-lighthouse-triageスキルに追記することを次アクションとして提案** | 分析中 | 2027-02-17 |
| FK-005 | 2026-08-09 | プロセス | 三名体制レビューは実施していたが、議事本体(3役の議論・ウタガイの反対理由)を「本セッションの会話記録を参照」と書いてgitに保存せず、設計書には裁定だけを転記した。2026-08-09〜08-13のglow-ma設計書を中心に、点検で計10本の保存漏れを検出 | 実装への実害は未確認。ただし規程3(ウタガイの反対理由が無い議事は無効)を満たさず、当時の判断を誰も検証できない状態が10本分発生 | 監査(2026-08-17 スキル・コマンド点検) | 三名体制の議事に**保存先の規定と検査が存在せず**、議事の所在が書き手の裁量に委ねられていた。会話に置いても誰も検知できない構造だった | テスト化: `scripts/check_triangle_review.py` + CI `docs-governance`(「会話記録を参照」型を検出し、既知10本はKNOWN_GAPSで固定して新規発生のみ落とすラチェット方式)/ ルール化: hojo-triangle-review・glow-ma-triangle-review 両スキルに保存先A/B/Cと軽量版(3役×各1行)を明記 | 「gitの外に置いた状態」という同型の穴を走査(下記の真因分析4を参照) | 監視中 | 2027-02-17 |
| FK-002 | 2026-07-23 | ヒヤリハット+プロセス | mainへのマージで競合ファイル一覧を確認せず、既知の2ファイルのみ--theirs解決して`git add -A`でコミット。競合が他ファイルに及んでいればマーカー入りHTMLを本番公開する構造だった(事後grepで無事を確認) | 実害なし(マーカー0・検査合格を事後確認) | 自己申告 | マージ解決手順が「競合一覧の確認→個別解決→マーカー検査→全検査→コミット」の順序で仕組み化されていない | ルール化: マージコミット前に①`git diff --name-only --diff-filter=U`で競合一覧確認 ②マーカーgrep ③check_lp+validate+node test合格、を必須手順として本台帳に記載(スクリプト化はメインブランチ運用が安定したら実施) | 企業のミカタ側のマージ運用にも同一ルールを適用 | 監視中 | 2027-01-23 |
| FK-001 | 2026-07-22 | ヒヤリハット+プロセス | LP検査がCSSの`max-width:100%`を禁止語『100%』と誤検知。さらに検査失敗を確認せずコミット・pushした(コマンドを`;`で連結し失敗が止まらなかった) | CI失敗1回。公開影響なし(未公開ブランチ) | 自己申告(CI実行前にローカルで検知) | ①検査対象に非表示領域(CSS/JS)を含めた設計の穴 ②検査→コミットの連結が失敗時に停止しない実行手順の穴 | テスト化: 検査を可視文言のみに修正(スクリプトに台帳ID明記)。ルール化: 検査系→コミットは`&&`連結を厳守し、失敗時はpushしない | 同型の穴を走査: validate_fukugiiro.py(JSON対象のため非該当)・SNS投稿検査(将来実装時に可視文言限定を仕様化) | 監視中 | 2027-01-22 |

## 記入ルール
- 48時間以内に記録(まず事実だけでよい。真因・対策は後から埋める)
- 分類: 事故 / 未達 / 商流 / ヒヤリハット / プロセス
- 発見経路: 自己申告 / 監査 / 機構スキャン / 顧客指摘 / 外部指摘(自己申告率が高いほど健全な組織と評価する)
- 行の削除は誰にもできない。クローズ後も残す(この台帳自体が組織の学習資産であり、新エージェント・新施策の必読資料)
- **技術修正・三名体制議事で解決済みでも記録は省略しない**(FK-003・FK-004は個別チームが議事で真因・対策まで完了させた後、本台帳への記録だけが素通りしていたことが2026-08-17の点検で判明。「直したから記録不要」は本台帳の原則「記録されなかった失敗は、必ずもう一度起きる」に反する)

---

## 真因分析: FK-005(フカボリさん)

【失敗ID】FK-005

**なぜ×5**

1. なぜ当時の判断を検証できないのか → 設計書に「裁定」しか残っておらず、ウタガイが何に反対したのかが読めないから
2. なぜ裁定しか残っていないのか → 議事本体を「本セッションの会話記録を参照」と書いて、gitの外に置いたから
3. なぜ会話の中に置いたのか → 議事の保存先が、憲法にもスキルにも一言も書かれておらず、書き手の裁量だったから
4. なぜ書かれていなかったのか → 2026-08-06までは偶然「別ファイル(`*-triangle-review.md`)で作る」運用が続き、問題が表面化しなかったから(暗黙知のまま regulation されなかった)
5. なぜ暗黙知のままだったのか → **保存漏れを検知する仕組みが無く、規程が破られても誰にも分からなかったから**

**真因(仕組みの穴として一文で)**
三名体制の議事に保存先の規定と検査が存在せず、議事が揮発しても検知されない構造だった。

**当初仮説の否定**
点検の初期には「議事が重いから急ぐと省略された」と仮説を立てたが、事実は違った。**議事は実施されている**(設計書が裁定1〜4を引用している)。消えたのは議事の保存先である。したがって対策を「フォーマットの軽量化」だけに置いていたら、真因を外していた。軽量版は補助的な対策として残す。

**対策(形態)**
- **テスト化**: `scripts/check_triangle_review.py`。「議事」の近傍に「会話記録/本セッションの記録/チャット履歴」がある書き方を検出する。既知10本は `KNOWN_GAPS` に台帳IDつきで固定し、**新規に増えた分だけ落とす**ラチェット方式。CI は `docs-governance`(specs配下の変更で起動)
- **ルール化**: hojo-triangle-review / glow-ma-triangle-review の両スキルに「議事の保存先(A ペアファイル / B パス参照 / C 設計書に直接)」と「軽量版=3役×各1行でも有効」を明記
- 遡っての議事作成は**行わない**。実装後に書くウタガイは結論を知ったうえでの後付けであり、規程の趣旨(実装前に止める)を果たさないまま記録だけが整う。該当10本には事実を1行追記して残した

**横展開: 同じ穴(gitの外に状態を置く)の走査結果**
- `docs/決裁キュー.md` が16日間未更新だった件も同根。決裁の現況が人の記憶と会話の側にあり、ファイルが追随していなかった → 2026-08-17に注意書きを追加済み。恒久対策は未着手(**残課題**)
- `context-limit-handoff` スキルが扱う領域そのもの。CLAUDE.mdのスキル運用表に「会話がコンテキスト上限に近い → context-limit-handoff」として明記済み
- **配布の穴(残課題)**: hojo-triangle-review / glow-ma-triangle-review は repo固有スキル扱いで `scripts/update-skills.sh` の `ALLGROUP_SKILL_NAMES` に入っていない。他repoで同型の議事揮発が起きても、この対策は届かない。ALLGROUP共通化するかは要判断

**実装担当部門**: 実装済み(検査スクリプト・CI・スキル改訂)。監視は ミハリさん。
**クローズ条件**: CI稼働のうえ `KNOWN_GAPS` が1行も増えないまま6ヶ月(2027-02-17判定)。
