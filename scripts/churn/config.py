"""早期解約リスク保全システムの定数・チューニング値。"""

EARLY_CHURN_MONTHS = 6

# 年代ビン
AGE_BANDS = [(0, 19, "〜10代"), (20, 29, "20代"), (30, 39, "30代"),
             (40, 49, "40代"), (50, 59, "50代"), (60, 200, "60代〜")]

# 金額ビンの境界（円）。商品特性に応じて小柳さん決裁で調整可。
AMOUNT_EDGES = [3000, 10000, 30000]
AMOUNT_LABELS = ["〜3千", "3千〜1万", "1万〜3万", "3万〜"]

# リスク要因として使う内部項目
FACTOR_FIELDS = ["product", "channel", "apply_form", "amount_band",
                 "age_band", "gender", "area", "agent_id"]

# 学習のスムージング強度（大きいほど少件数を全体平均へ強く引き寄せる）
SMOOTHING_K = 30

# 段階導入（先行/後発）で「先行群（保全を先に受ける側）」へ割り付ける比率。
# 因果効果を測るための対照群設計。実運用の比率・ウェーブ設計は小柳さん決裁事項（決裁待ち）。
EXPERIMENT_TREATED_FRACTION = 0.5
# 「参考値」と注記する最小件数（これ未満は母数不足）
MIN_RELIABLE_N = 20

# リスク帯の閾値（ベース解約率に対する倍率）
BAND_HIGH_MULT = 2.0
BAND_LOW_MULT = 1.0

# 高帯のしきい値の上限。base_rateが高くても高リスクを取りこぼさない
HIGH_CUTOFF_CEILING = 0.9

# --- 顧客カルテ(接触履歴・放置検知) ---
# 高リスクで直近この日数、接触が無ければ「要フォロー(放置)」とみなす
FOLLOWUP_DAYS = 14

# 初動トリガー: 契約後この日数以内に一度も接触が無い継続契約を「初動要接触」に出す。
# 保全は早いほど効く＝オンボーディングの取りこぼしを拾う。窓の日数は小柳さん決裁事項（決裁待ち）。
INITIAL_CONTACT_DAYS = 14

# 前提（小柳さん決裁 2026-08-11）: 顧客名は持ち込まず**顧客IDで管理**する。
# column_map にこれらの氏名系キーを載せることを門前（preflight）で禁止する（データ最小化）。
# 比較は小文字化して行う（英字キーの大小無視）。
FORBIDDEN_NAME_KEYS = {
    "name", "customer_name", "full_name", "insured_name", "contractor_name",
    "顧客名", "氏名", "名前", "なまえ", "お名前", "被保険者名", "契約者名",
}

# 月次スナップショットの保持年数（守り部審査 #142 項目3）。満期を過ぎたら確実削除する。
# 小柳さん指示 2026-08-11＝6年。**保持期限の適法性は守り部・外部専門家レビュー待ち**。
SNAPSHOT_RETENTION_YEARS = 6

# 口座“普段使い”フラグの想定ドメイン（守り部審査 #142 項目4）。空欄＝未入力は対象外。
# preflightで想定外の値を「件数だけ」警告する（値は出さない・停止条件にはしない）。
ACCOUNT_DAILY_DOMAIN = {"はい", "いいえ", "未確認"}

# 予防トリガー: 引落予定日がこの日数以内に来る＝「引落前」とみなし口座確認の対象にする
# （≒翌月の初回引落まで先読み）。実運用の発火タイミングは小柳さん決裁事項。
PRE_DEBIT_LEAD_DAYS = 40

# 「今日の要接触」の1日あたり対応キャパ（件）。現場の処理能力＝小柳さん決裁事項。
CAPACITY_PER_DAY = 30

# 日次パイプラインの品質ゲート。バックテストAUCがこれ未満なら“publishせず安全停止”。
# 「当たらないモデルで現場を動かさない」ための下限。値は小柳さん決裁事項（決裁待ち）。
AUC_MIN = 0.65

# 較正の品質しきい値。ECE（期待較正誤差）がこれを超えたら「リスク%が信用できない」と警告。
# 当たっても水準がズレると保全の優先度づけを誤るため。値は小柳さん決裁事項（決裁待ち）。
CALIBRATION_ECE_WARN = 0.10
# 「追加案内 ◯回」に数える接触種別
ADDITIONAL_GUIDANCE_KINDS = {"追加案内"}
# 接触種別の表記ゆれ → 正規化。未知の値は原文を保持する
INTERACTION_KIND_MAP = {
    "架電": "架電", "電話": "架電", "TEL": "架電", "tel": "架電",
    "案内": "案内", "追加案内": "追加案内", "来店": "来店",
    "メール": "メール", "mail": "メール", "面談": "面談",
}
